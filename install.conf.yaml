- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim 
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux


- shell:
  - [git submodule update --init --recursive, Installing submodules]

