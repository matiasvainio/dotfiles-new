- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: 
      path: nvim 
    ~/.zshrc: 
      path: zshrc
    ~/.tmux.conf:
      path: tmux

- shell:
  - [git submodule update --init --recursive, Installing submodules]

